# ─── Source unique DATABASE_URL (monorepo) ───
# Utilisé par : Prisma CLI, NestJS API, seed
# En production : remplacer par l'URL PgBouncer / managed DB
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/khadamat?schema=public&connection_limit=10&pool_timeout=10"
