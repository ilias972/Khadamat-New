# Database
DATABASE_URL="postgresql://user:password@localhost:5432/khadamat?schema=public"

# API
API_PORT=3001
NODE_ENV=development

# Frontend
FRONTEND_URL=http://localhost:3000

# JWT
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Payment (Manual MVP)
# Pour le MVP, les paiements sont manuels (virement, cash, etc.)
# Un admin valide les paiements via POST /api/payment/admin/confirm/:oid
PAYMENT_CONTACT_PHONE="+212 6XX XXX XXX"
PAYMENT_CONTACT_EMAIL="paiement@khadamat.ma"
